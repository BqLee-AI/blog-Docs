## 1. OpenSpec Baseline

- [x] 1.1 对齐 `openspec/config.yaml`，使其反映控制面仓库的长期上下文与 artifact 规则
- [x] 1.2 创建首条治理 change，作为文档仓库采用 OpenSpec 的基线样板

## 2. Capability Specs

- [x] 2.1 为控制面文档写法建立 `control-plane-docs` 能力规范
- [x] 2.2 为治理变更流程建立 `spec-driven-governance` 能力规范

## 3. Validation

- [x] 3.1 运行 `openspec validate establish-control-plane-specs`
- [x] 3.2 归档该 change，将能力规范写入 `openspec/specs/`
