## 1. 背景

Week 3 的内容与风控链路需要延期处理，候选版本在完成 OpenSpec 前置前不得继续吸收该范围内的新实现。

## 2. 目标

- 明确 FE 发布壳的冻结边界。
- 确保 FE-009 / FE-010 / FE-011 / FE-012 在完成 OpenSpec 前置与验证口径对齐前，不进入当前候选版本。
- 将构建、smoke 与回滚口径作为进入候选版本的必要前提。

## 3. 范围
- In Scope:
  - FE-009
  - FE-010
  - FE-011
  - FE-012
- Out of Scope:
  - FE-009 / FE-010 / FE-011 / FE-012 的直接实现合入当前候选版本
  - FE-013 及更后续功能扩展

## 4. 契约
- API / 数据结构 / 错误语义：
  - 当前冻结状态下不允许通过实现分支改变业务契约
  - 若契约必须变更，必须先走新的 OpenSpec change 并补齐回滚口径

## 5. 验收标准
- [ ] 冻结边界明确且可追踪
- [ ] Harness 计划包含构建、关键 smoke 与回滚证据
- [ ] 候选版本外的内容不会进入当前发布壳
- [ ] 延后项与回滚路径在低权重同步记录中可复核

## 6. 风险与回滚
- 风险:
  - 边界不清晰会导致候选版本持续漂移
  - 缺少回滚口径会让冻结边界失去可执行性
- 回滚:
  - 维持当前冻结边界，不推进 FE-009 / FE-012 进入实现分支
  - 如需恢复推进，必须先创建新 change 调整边界

## 7. 关联 Issue
- FE:
  - W3-FE-FRZ-11
- BE:
  - 无
